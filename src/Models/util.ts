export interface UtilState {
    notificationTotal: number;
    refreshProfile: any;
  }
  export interface ContactForm {
    name: string;
    email: string;
    number: string;
    location: string;
    message: string;
  }
  